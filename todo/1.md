# TypeScript 编译器 Haxe 特性支持需求文档

## 1. 项目概述

### 1.1 项目目标
扩展 TypeScript 编译器，支持 Haxe 语言的核心特性，提升开发体验和代码表达能力。

### 1.2 项目范围
本需求涵盖 10 个 Haxe 关键特性的 TypeScript 实现。

---

## 2. 功能需求

### 2.1 范围迭代语法 (Range-based For Loop)

**需求描述**：支持 Haxe 风格的范围迭代语法

**语法规范**：
```typescript
// 基础范围迭代
for (i in 0...10) {
  console.log(i); // 输出 0 到 9
}

// 带步长的迭代
for (i in (0...100).stepBy(5)) {
  console.log(i); // 输出 0, 5, 10, ..., 95
}

// 包含上界的迭代（可选） 不要支持
// for (i in 0...=10) {
//   console.log(i); // 输出 0 到 10
// }
```

**技术要求**：
- 编译器需在 AST 解析阶段识别 `in x...y` 语法
- 转译为标准的 `for` 循环或 `Array.from` + `forEach`
- `stepBy` 方法需返回可迭代对象
- 支持负数和倒序迭代

**输出示例**：
```javascript
// 转译后
for (let i = 0; i < 10; i++) {
  console.log(i);
}
```

---

### 2.2 增强枚举 (Enhanced Enums)

**需求描述**：支持携带数据的代数枚举类型

**语法规范**：
```typescript
enum Result<T, E> {
  Ok(value: T),
  Err(error: E)
}

enum Option<T> {
  Some(value: T),
  None
}

// 使用示例
const result: Result<number, string> = Result.Ok(42);
const empty: Option<string> = Option.None;
```

**技术要求**：
- 枚举变体可携带命名参数
- 支持泛型参数
- 编译时类型检查
- 生成构造函数和类型守卫

**运行时实现**：
```javascript
class Result {
  static Ok(value) {
    return { _tag: 'Ok', value };
  }
  static Err(error) {
    return { _tag: 'Err', error };
  }
}
```

---

### 2.3 属性访问器 (Property Accessors)

**需求描述**：支持 Haxe 风格的 getter/setter 声明

**语法规范**：
```typescript
class Rectangle {
  private _width: number = 0;
  private _height: number = 0;

  // Haxe 风格
  public var width(get, set): number;
  public var height(get, set): number;
  public var area(get, never): number;

  function get_width(): number {
    return this._width;
  }

  function set_width(value: number): number {
    return this._width = Math.max(0, value);
  }

  function get_area(): number {
    return this._width * this._height;
  }
}
```

**访问权限修饰符**：
- `(get, set)` - 可读写
- `(get, never)` - 只读
- `(never, set)` - 只写
- `(default, default)` - 公开字段
- `(default, null)` - 可初始化后只读

**技术要求**：
- 编译为 ES5 getter/setter 或私有字段
- 支持访问权限控制
- 类型推断支持

---

### 2.4 类型别名 (Type Definitions)

**需求描述**：增强的类型定义能力

**语法规范**：
```typescript
// 结构类型别名
typedef Point = {
  x: number;
  y: number;
}

// 函数类型别名
typedef Callback<T> = (data: T) => void;

// 带约束的类型别名
typedef Numeric = number | bigint;

// 扩展类型
typedef Point3D = Point & {
  z: number;
}
```

**与 type 关键字的区别**：
- `typedef` 强调结构等价性
- 支持类型扩展和组合
- 更好的编辑器提示

**技术要求**：
- 完全编译时特性
- 支持递归类型定义
- 与现有 `type` 关键字互操作

---

### 2.5 抽象类型 (Abstract Types)

**需求描述**：零成本包装类型，编译时类型安全

**语法规范**：
```typescript
abstract Meter(number) {
  inline function new(value: number) {
    this = value;
  }

  @:op(A + B)
  static function add(a: Meter, b: Meter): Meter {
    return new Meter(a.toNumber() + b.toNumber());
  }

  public inline function toNumber(): number {
    return this;
  }
}

// 使用
const distance: Meter = new Meter(100);
const total = distance + new Meter(50); // 类型安全
```

**关键特性**：
- 运行时零开销（编译后为基础类型）
- 编译时类型区分
- 支持运算符重载
- 隐式/显式转换控制

**技术要求**：
- 使用 branded types 实现
- 内联函数完全消除包装
- 运算符重载通过方法调用实现

---

### 2.6 模式匹配 (Pattern Matching)

**需求描述**：表达式级的模式匹配

**语法规范**：
```typescript
const value = match (result) {
  Result.Ok(v) if v > 0 => `Positive: ${v}`,
  Result.Ok(0) => "Zero",
  Result.Ok(v) => `Negative: ${v}`,
  Result.Err(e) => `Error: ${e}`,
  _ => "Unknown"
};

// 解构匹配
match (point) {
  { x: 0, y: 0 } => "Origin",
  { x, y } if x === y => "Diagonal",
  { x: 0, y } => `Y-axis at ${y}`,
  { x, y: 0 } => `X-axis at ${x}`,
  _ => "Other"
}
```

**支持的模式**：
- 字面量匹配
- 枚举变体匹配
- 解构匹配（对象/数组）
- 守卫条件（`if`）
- 通配符（`_`）
- 变量绑定

**技术要求**：
- 穷尽性检查（exhaustiveness checking）
- 编译为 if-else 或 switch
- 返回表达式值

---

### 2.7 枚举作为代数数据类型 (Enums as ADTs)

**需求描述**：完整的 ADT 支持（已在 2.2 中部分覆盖）

**扩展要求**：
```typescript
enum BinaryTree<T> {
  Leaf,
  Node(left: BinaryTree<T>, value: T, right: BinaryTree<T>)
}

// 递归处理
function depth<T>(tree: BinaryTree<T>): number {
  return match (tree) {
    BinaryTree.Leaf => 0,
    BinaryTree.Node(left, _, right) => 
      1 + Math.max(depth(left), depth(right))
  };
}
```

**技术要求**：
- 支持递归类型定义
- 与模式匹配深度集成
- 优化的内存表示

---

### 2.8 标称类型系统 (Nominal Typing)

**需求描述**：可选的标称类型标记

**语法规范**：
```typescript
@nominal
class UserId {
  constructor(private id: string) {}
}

@nominal
class ProductId {
  constructor(private id: string) {}
}

// 即使结构相同也不能互换
const userId: UserId = new UserId("user-123");
const productId: ProductId = userId; // ❌ 编译错误
```

**技术要求**：
- 通过装饰器或关键字标记
- 编译时强制类型唯一性
- 可与结构类型混用
- 支持类、接口、抽象类型

**实现方式**：
```typescript
// 内部实现
type Nominal<T, Brand> = T & { __brand: Brand };
type UserId = Nominal<string, 'UserId'>;
```

---

### 2.9 内联函数 (Inline Functions)

**需求描述**：编译时函数内联优化

**语法规范**：
```typescript
inline function square(x: number): number {
  return x * x;
}

inline function clamp(value: number, min: number, max: number): number {
  return Math.min(Math.max(value, min), max);
}

// 调用
const result = square(5) + clamp(10, 0, 100);

// 编译后
const result = 5 * 5 + Math.min(Math.max(10, 0), 100);
```

**技术要求**：
- 在 AST 转换阶段内联
- 支持泛型函数内联
- 避免副作用时才内联
- 编译选项控制内联策略
- 递归调用不内联

**限制条件**：
- 函数体不超过配置的行数
- 不包含复杂控制流
- 参数无副作用

---

### 2.10 静态扩展 (Static Extensions)

**需求描述**：为现有类型添加方法而不修改原型

**语法规范**：
```typescript
// 定义扩展
static extension StringTools {
  static function reverse(str: string): string {
    return str.split('').reverse().join('');
  }

  static function capitalize(str: string): string {
    return str.charAt(0).toUpperCase() + str.slice(1);
  }
}

// 使用（编译时转换）
using StringTools;

const reversed = "hello".reverse(); // ✅
const capped = "world".capitalize(); // ✅
```

**技术要求**：
- `using` 语句导入扩展
- 作用域限制（文件/块级）
- 编译为静态方法调用：`StringTools.reverse("hello")`
- 支持泛型扩展
- 类型推断保持

**与 TypeScript 现有特性的区别**：
| 特性 | 静态扩展 | 原型扩展 |
|------|----------|----------|
| 运行时影响 | 无 | 修改全局原型 |
| 作用域 | 局部 | 全局 |
| 性能 | 无开销 | 原型链查找 |

---

## 3. 非功能需求

### 3.1 性能要求
- 编译时间增加不超过 20%
- 运行时零开销（内联、抽象类型）
- 支持增量编译

### 3.2 兼容性要求
- 与现有 TypeScript 代码 100% 兼容
- 可通过配置开关启用/禁用特性
- 目标 ES2015+ 输出

### 3.3 开发体验
- 完整的 IDE 支持（LSP）
- 错误提示清晰准确
- 代码补全和重构支持

---

## 4. 配置选项

### 4.1 tsconfig.json 扩展
```json
{
  "compilerOptions": {
    "haxeFeatures": {
      "rangeIteration": true,
      "enhancedEnums": true,
      "propertyAccessors": true,
      "abstractTypes": true,
      "patternMatching": true,
      "nominalTyping": false,
      "inlineFunctions": "auto",
      "staticExtensions": true
    }
  }
}
```

---

## 5. 实现优先级

### P0（核心功能）
1. 范围迭代语法
2. 增强枚举
3. 模式匹配

### P1（重要功能）
4. 属性访问器
5. 抽象类型
6. 静态扩展

### P2（增强功能）
7. 类型别名增强
8. 内联函数
9. 标称类型系统
10. 枚举 ADT 优化

---

## 6. 测试要求

### 6.1 单元测试
- 每个特性覆盖率 > 90%
- 边界情况测试
- 错误处理测试

### 6.2 集成测试
- 特性组合测试
- 大型项目编译测试
- 性能基准测试

### 6.3 兼容性测试
- 与现有 TS 项目集成
- 第三方库兼容性
- 不同 target 输出测试

---

## 7. 文档要求

1. **语言规范文档**：每个特性的详细语法说明
2. **迁移指南**：从纯 TypeScript 迁移的指导
3. **API 参考**：编译器 API 变更说明
4. **示例代码库**：实际用例展示

---

## 8. 风险评估

| 风险 | 